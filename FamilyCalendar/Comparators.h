// Comparators.h
// A header file not used to store a class, but standalone functions as comparators. Names should be given accordingly.
//
#include "Event.h"

// compEventByDate
bool compEventByDate(const Event &eventOne, const Event &eventTwo);